public Boolean isBirthday(Contact c) {
    //code here
    Boolean isBirthDate = false;
    if(system.Today().Month() == c.Birthdate.Month() &
        system.Today().Day() == c.Birthdate.Day()){
            isBirthDate = true;
        }
    return isBirthDate;
}